import { toFahrenheit } from './conversion';
// const sum = require("./Conversion");

// eslint-disable-next-line no-undef
test('should convert celsius to fahrenheit', () => {
  // eslint-disable-next-line no-undef
  expect(toFahrenheit(10)).toBe(50);
});

// eslint-disable-next-line no-undef
describe('index', () => {
  // eslint-disable-next-line no-undef
  it('should work', () => {
    // eslint-disable-next-line no-undef
    expect(true).toBeTruthy();
  });
});
